<Page
    x:Class="Portfolio_Tracker.Views.CryptoMarketPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{DynamicResource MainBackground}"
    Loaded="Page_Loaded"
    Opacity="0">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="28"
            FontWeight="Bold"
            Foreground="{DynamicResource TextColor}"
            Text="Global Cryptocurrency Market" />

        <StackPanel
            Grid.Row="1"
            Margin="0,10,0,10"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <TextBlock
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Foreground="{DynamicResource TextColor}"
                Text="Search" />
            <TextBox
                Width="220"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Background="White"
                FontSize="14"
                Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Margin="0,0,8,0"
                Padding="8"
                Command="{Binding RefreshCommand}"
                Content="Refresh" />
            <TextBlock
                VerticalAlignment="Center"
                Foreground="Gray"
                Text="{Binding LastUpdatedDisplay}" />
        </StackPanel>

        <Border
            Grid.Row="2"
            Padding="10"
            Background="{DynamicResource CardBackground}"
            BorderBrush="#2E2E4F"
            BorderThickness="1"
            CornerRadius="8">
            <DataGrid
                AlternatingRowBackground="White"
                AutoGenerateColumns="False"
                Background="White"
                FontSize="14"
                Foreground="{DynamicResource TableTextColor}"
                IsReadOnly="True"
                ItemsSource="{Binding Coins}"
                RowBackground="White"
                SelectedItem="{Binding SelectedCoin, Mode=TwoWay}"
                SelectionMode="Single">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White" />
                        <Setter Property="Foreground" Value="{DynamicResource TableTextColor}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="48" Header="">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image
                                    Width="28"
                                    Height="28"
                                    Source="{Binding ImageUrl}"
                                    Stretch="Uniform" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn
                        Width="80"
                        Binding="{Binding Symbol}"
                        Header="Symbol" />
                    <DataGridTextColumn
                        Width="2*"
                        Binding="{Binding Name}"
                        Header="Name" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding CurrentPrice, StringFormat=N2}"
                        Header="Price (USD)" />
                    <DataGridTextColumn
                        Width="140"
                        Binding="{Binding MarketCap, StringFormat=N0}"
                        Header="Market Cap" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding Change24h, StringFormat=N2}"
                        Header="24h %" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>